import test from"node:test";import assert from"node:assert/strict";import{evaluateProductionProfile}from"../app/business-builder/production-profile.mjs";
test("low-cost production profile is ready only with required durable services and recovery",()=>{const r=evaluateProductionProfile({postgresql:true,objectStorage:true,backupEncrypted:true,restoreDrillVerified:true});assert.equal(r.ready,true);assert.deepEqual(r.blockers,[]);});
test("unsafe expensive production defaults fail closed",()=>{const r=evaluateProductionProfile({postgresql:true,objectStorage:true,backupEncrypted:false,restoreDrillVerified:false,hostDockerSocket:true,privileged:true,perTenantAlwaysOn:true});assert.equal(r.ready,false);for(const id of["host-docker-socket-forbidden","privileged-runtime-forbidden","per-tenant-always-on-forbidden","encrypted-backup-required","restore-drill-required"])assert.ok(r.blockers.includes(id));});
