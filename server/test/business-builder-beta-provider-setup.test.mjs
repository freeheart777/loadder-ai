import test from"node:test";import assert from"node:assert/strict";import{evaluateBetaProviderSetup,publicBetaProviderSetup}from"../app/business-builder/beta-provider-setup.mjs";
test("beta provider setup fails closed without external provider references",()=>{const r=evaluateBetaProviderSetup({});assert.equal(r.ready,false);assert.ok(r.blockers.includes("postgres.connectionSecretRef"));assert.ok(r.blockers.includes("objectStorage.credentialSecretRef"));assert.ok(r.blockers.includes("oauth.clientSecretRef"));});
test("beta provider setup accepts secret references and health contracts without exposing secret values",()=>{const config={postgres:{connectionSecretRef:"secret://postgres/beta"},externalSecrets:{provider:"external",healthCheck:async()=>({ok:true})},objectStorage:{provider:"s3-compatible",bucket:"loadder-beta",credentialSecretRef:"secret://storage/beta"},oauth:{provider:"google",clientIdSecretRef:"secret://oauth/client-id",clientSecretRef:"secret://oauth/client-secret",redirectUri:"https://beta.example.com/oauth/callback"}};const r=publicBetaProviderSetup(config);assert.equal(r.ready,true);const encoded=JSON.stringify(r);assert.equal(encoded.includes("secret://postgres/beta"),false);assert.equal(encoded.includes("secret://oauth/client-secret"),false);});
