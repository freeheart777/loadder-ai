import test from"node:test";import assert from"node:assert/strict";import{readFileSync}from"node:fs";const read=p=>readFileSync(new URL(p,import.meta.url),"utf8");
test("preview studio stays internal, responsive and explicitly unpublished",()=>{const frame=read("../../src/components/business-builder/PreviewStudioFrame.tsx"),page=read("../../src/pages/BusinessBuilderPage.tsx"),router=read("../app/routes/business-builder.mjs");assert.match(frame,/data-preview-studio="true"/);assert.match(frame,/منتشر نشده/);assert.match(frame,/Desktop preview/);assert.match(frame,/Tablet preview/);assert.match(frame,/Mobile preview/);assert.match(frame,/sandbox="allow-same-origin"/);assert.doesNotMatch(frame,/allow-scripts|allow-forms|allow-popups|allow-top-navigation/);assert.match(frame,/Preview به معنی Publish نیست/);assert.match(page,/PreviewStudioFrame/);assert.match(page,/setPreviewUrl\(""\)/);assert.match(router,/form-action 'none'/);assert.match(router,/frame-ancestors 'self'/);});
