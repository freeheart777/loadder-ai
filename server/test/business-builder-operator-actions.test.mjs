import test from"node:test";import assert from"node:assert/strict";import{recommendLoadderActions,executeOwnedActionDraft}from"../app/business-builder/action-recommender.mjs";
test("inventory risk becomes approval-gated procurement draft",()=>{const actions=recommendLoadderActions({intelligence:{kind:"inventory",alerts:[{entityId:"stock",recordId:"s1"}]}});assert.equal(actions.length,1);assert.equal(actions[0].requiresApproval,true);const draft=executeOwnedActionDraft({action:actions[0],record:{id:"s1",reorderPoint:12}});assert.equal(draft.status,"draft");assert.equal(draft.next,"human-approval");assert.equal(draft.payload.quantity,12)});
test("sales overdue activity becomes draft, never automatic external action",()=>{const a=recommendLoadderActions({intelligence:{kind:"crm",alerts:[{entityId:"activity",recordId:"a1"}]}})[0];const d=executeOwnedActionDraft({action:a,record:{id:"a1"}});assert.equal(d.status,"draft");assert.match(d.payload.message,/پیگیری/);assert.equal(d.next,"human-approval")});
