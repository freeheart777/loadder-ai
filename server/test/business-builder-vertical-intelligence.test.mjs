import test from "node:test";import assert from "node:assert/strict";import { loadderBusinessCompiler } from "../app/business-builder/business-compiler.mjs";import { buildVerticalIntelligence } from "../app/business-builder/vertical-intelligence.mjs";
test("CRM intelligence computes pipeline and win rate",()=>{const definition=loadderBusinessCompiler.compile({intent:"CRM فروش بساز",name:"CRM"});const x=buildVerticalIntelligence({definition,snapshot:{customer:[{id:"c1"}],opportunity:[{id:"o1",stage:"won",value:100},{id:"o2",stage:"proposal",value:250}],activity:[]}});assert.equal(x.kind,"crm");assert.equal(x.cards.find(c=>c.id==="pipeline").value,250);assert.equal(x.cards.find(c=>c.id==="conversion").value,50);});
test("Inventory intelligence detects reorder risk",()=>{const definition=loadderBusinessCompiler.compile({intent:"انبار و موجودی بساز",name:"Inventory"});const x=buildVerticalIntelligence({definition,snapshot:{product:[{id:"p1"}],warehouse:[{id:"w1"}],stock:[{id:"s1",quantity:2,reorderPoint:5}]}});assert.equal(x.kind,"inventory");assert.equal(x.cards.find(c=>c.id==="low").value,1);assert.equal(x.alerts.length,1);});
test("Booking intelligence computes upcoming bookings",()=>{const definition=loadderBusinessCompiler.compile({intent:"سیستم رزرو بساز",name:"Booking"});const tomorrow=new Date(Date.now()+86400000).toISOString();const x=buildVerticalIntelligence({definition,snapshot:{booking:[{id:"b1",startsAt:tomorrow,status:"confirmed"}],service:[{id:"s1"}],customer:[]}});assert.equal(x.kind,"booking");assert.equal(x.cards.find(c=>c.id==="upcoming").value,1);});
