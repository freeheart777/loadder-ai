import test from'node:test';import assert from'node:assert/strict';import{planBlueprintUpgrade,assertUpgradeAllowed}from'../app/business-builder/app-store-upgrade.mjs';test('additive blueprint upgrades are safe',()=>{const p=planBlueprintUpgrade({currentDefinition:{entities:[{id:'lead',fields:[{id:'name',type:'string'}]}]},nextDefinition:{entities:[{id:'lead',fields:[{id:'name',type:'string'},{id:'phone',type:'phone'}]}]}});assert.equal(p.safe,true);assert.equal(p.destructiveCount,0);assert.equal(assertUpgradeAllowed(p),true);});test('destructive upgrades fail closed without explicit approval',()=>{const p=planBlueprintUpgrade({currentDefinition:{entities:[{id:'lead',fields:[{id:'name',type:'string'}]}]},nextDefinition:{entities:[{id:'lead',fields:[]}]}});assert.equal(p.safe,false);assert.equal(p.requiresApproval,true);assert.throws(()=>assertUpgradeAllowed(p),/explicit approval/);assert.equal(assertUpgradeAllowed(p,{approvedDestructive:true}),true);});
