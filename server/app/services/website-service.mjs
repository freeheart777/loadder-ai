import{decodeCursor}from"../query/cursor-pagination.mjs";import{LANDING_VERSIONS,landingHash,validateLandingBlueprint}from"../landing/landing-contracts.mjs";
const roles=new Set(["owner","admin","member"]),types=new Set(["CORPORATE","PROFESSIONAL_SERVICE","MEDICAL","LEGAL","SERVICE","CATALOG"]),pageTypes=new Set(["HOME","ABOUT","SERVICES","SERVICE_DETAIL","CONTACT","FAQ","BLOG_INDEX","CUSTOM_PAGE"]),plain=v=>v!==null&&typeof v==="object"&&!Array.isArray(v),safeTheme=v=>plain(v)&&Object.keys(v).every(k=>["font","primaryColor","secondaryColor","backgroundColor","foregroundColor","mutedColor","radius","spacingDensity","buttonStyle","containerWidth"].includes(k))&&["primaryColor","secondaryColor","backgroundColor","foregroundColor","mutedColor"].every(k=>/^#[0-9a-fA-F]{6}$/.test(v[k]))&&["brand","sans","serif"].includes(v.font)&&["compact","comfortable","spacious"].includes(v.spacingDensity)&&["solid","outline","soft"].includes(v.buttonStyle)&&["narrow","standard","wide"].includes(v.containerWidth)&&["sm","md","lg","pill"].includes(v.radius);
export class WebsiteError extends Error{constructor(code,status=400){super(code);this.code=code;this.status=status;}}const fail=(c,s)=>{throw new WebsiteError(c,s)},key=v=>{if(typeof v!=="string"||!v.trim()||v.length>200)fail("WEBSITE_INVALID");return v.trim();},permission=a=>{if(!a?.userId||!roles.has(a.role))fail("WEBSITE_PERMISSION_DENIED",403);},strict=(v,allowed)=>plain(v)&&Object.keys(v).every(k=>allowed.includes(k)),slug=v=>typeof v==="string"&&/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(v)&&v.length<=80,path=v=>typeof v==="string"&&/^\/(?:[a-z0-9]+(?:-[a-z0-9]+)*\/?)?$/.test(v)&&v.length<=160;
export function createWebsiteService({repository,contextRepository,placementRepository,assetRepository,componentRegistry,presetRegistry,publisher,now=()=>new Date()}){const project=id=>repository.findProject(id)||fail("WEBSITE_NOT_FOUND",404),page=id=>repository.findPage(id)||fail("WEBSITE_PAGE_NOT_FOUND",404),blueprint=id=>repository.findBlueprint(id)||fail("WEBSITE_BLUEPRINT_NOT_FOUND",404),hash=landingHash;return Object.freeze({
 readiness(){return Object.freeze({websiteAuthoringReady:true,websitePreviewReady:true,websitePublicationConfigured:publisher.configured,websiteAiGenerationReady:false,websiteCommerceReady:false});},presets(){return{presets:presetRegistry.list()};},
 createProject(input,actor,rawKey){permission(actor);if(!strict(input,["name","slug","websiteType","presetId","businessContextVersionId","locale","direction","theme"])||typeof input.name!=="string"||!input.name.trim()||input.name.length>200||!slug(input.slug)||!types.has(input.websiteType)||typeof input.businessContextVersionId!=="string"||!contextRepository.getVersion(input.businessContextVersionId)||!safeTheme(input.theme)||input.locale!=="fa-IR"||input.direction!=="rtl")fail("WEBSITE_INVALID");const preset=presetRegistry.get(input.presetId);if(!preset||preset.websiteType!==input.websiteType)fail("WEBSITE_PRESET_INVALID");const normalized={name:input.name.trim(),slug:input.slug,websiteType:input.websiteType,presetId:preset.presetId,presetVersion:preset.presetVersion,businessContextVersionId:input.businessContextVersionId,locale:input.locale,direction:input.direction,theme:input.theme},result=repository.createProject({...normalized,userId:actor.userId,key:key(rawKey),requestHash:hash(normalized),now:now().toISOString()});if(!result.created&&result.value.requestHash!==hash(normalized))fail("WEBSITE_IDEMPOTENCY_CONFLICT",409);return{website:result.value,reusedResult:!result.created};},
 listProjects(query,actor){permission(actor);if(Object.keys(query||{}).some(k=>!["limit","cursor"].includes(k)))fail("WEBSITE_INVALID");const limit=query?.limit===undefined?20:Number(query.limit);if(!Number.isInteger(limit)||limit<1||limit>100)fail("WEBSITE_INVALID");let cursor;try{cursor=decodeCursor(query?.cursor,"website_projects",["updatedAt","id"]);}catch{fail("WEBSITE_INVALID");}const r=repository.listProjects({limit,cursor});return{websites:r.items,nextCursor:r.nextCursor};},getProject(id,actor){permission(actor);return{website:project(id)};},
 createPage(projectId,input,actor,rawKey){permission(actor);const p=project(projectId);if(!strict(input,["pageType","name","path","navigationVisible","navigationOrder"])||!pageTypes.has(input.pageType)||typeof input.name!=="string"||!input.name.trim()||input.name.length>160||!path(input.path)||typeof input.navigationVisible!=="boolean"||!Number.isInteger(input.navigationOrder)||input.navigationOrder<0||input.navigationOrder>1000)fail("WEBSITE_INVALID");const normalized={websiteProjectId:p.id,pageType:input.pageType,name:input.name.trim(),path:input.path,navigationVisible:input.navigationVisible,navigationOrder:input.navigationOrder},result=repository.createPage({...normalized,userId:actor.userId,key:key(rawKey),requestHash:hash(normalized),now:now().toISOString()});if(!result.created&&result.value.requestHash!==hash(normalized))fail("WEBSITE_IDEMPOTENCY_CONFLICT",409);return{page:result.value,reusedResult:!result.created};},listPages(projectId,actor){permission(actor);project(projectId);return{pages:repository.listPages(projectId)};},
 createBlueprint(pageId,input,actor,rawKey){permission(actor);const pg=page(pageId),p=project(pg.websiteProjectId);if(!strict(input,["blueprint","supersedesBlueprintId"]))fail("WEBSITE_INVALID");const supersedes=input.supersedesBlueprintId??pg.currentDraftBlueprintId??null;if(supersedes!==null&&blueprint(supersedes).websitePageId!==pg.id)fail("WEBSITE_BLUEPRINT_NOT_FOUND",404);let value;try{value=validateLandingBlueprint(input.blueprint,componentRegistry);}catch{fail("WEBSITE_BLUEPRINT_INVALID");}if(JSON.stringify(value.designTokens)!==JSON.stringify(p.theme))fail("WEBSITE_THEME_MISMATCH");for(const section of value.sections){for(const b of section.contentBindings)if(!placementRepository.findById(b.creativePlacementId))fail("CREATIVE_PLACEMENT_NOT_FOUND",404);for(const b of section.assetBindings){const asset=assetRepository.findById(b.contentAssetId);if(!asset||asset.status!=="READY")fail("CONTENT_ASSET_NOT_FOUND",404);}}const normalized={websiteProjectId:p.id,websitePageId:pg.id,blueprint:value,supersedesBlueprintId:supersedes,...LANDING_VERSIONS},result=repository.createBlueprint({...normalized,userId:actor.userId,locale:p.locale,direction:p.direction,key:key(rawKey),contentHash:hash(value),requestHash:hash(normalized),now:now().toISOString()});if(!result.created&&result.value.requestHash!==hash(normalized))fail("WEBSITE_IDEMPOTENCY_CONFLICT",409);return{blueprint:result.value,reusedResult:!result.created};},listBlueprints(pageId,actor){permission(actor);page(pageId);return{blueprints:repository.listBlueprints(pageId)};},
 publish(projectId,_input,actor,rawKey){permission(actor);const p=project(projectId),pages=repository.listPages(p.id);if(!pages.length)fail("WEBSITE_PAGES_REQUIRED");const lineage=pages.map(pg=>{if(!pg.currentDraftBlueprintId)fail("WEBSITE_BLUEPRINT_REQUIRED");return{page:pg,blueprint:blueprint(pg.currentDraftBlueprintId)};}),artifact=publisher.publish({project:p,pages:lineage}),normalized={websiteProjectId:p.id,pageBlueprintIds:lineage.map(x=>x.blueprint.id)},requestHash=hash(normalized),at=now().toISOString(),result=repository.publish({websiteProjectId:p.id,userId:actor.userId,manifest:artifact.available?artifact.manifest:{schemaVersion:1,pages:[]},manifestHash:artifact.available?artifact.manifestHash:hash({schemaVersion:1,pages:[]}),host:artifact.available?artifact.host:null,basePath:artifact.available?artifact.basePath:null,status:artifact.available?"PUBLISHED":"FAILED",failureCode:artifact.available?null:artifact.failureCode,key:key(rawKey),requestHash,now:at});if(!result.created&&result.value.requestHash!==requestHash)fail("WEBSITE_IDEMPOTENCY_CONFLICT",409);if(!artifact.available)fail("WEBSITE_PUBLICATION_UNAVAILABLE",503);return{publication:result.value,publicUrl:publisher.publicUrl(result.value),reusedResult:!result.created};}
});}
