import{createInternalTqmService,InternalTqmError}from"./internal-tqm-service.mjs";
export function createGovernedInternalTqmService({source,pdcaService}){const base=createInternalTqmService({source});return Object.freeze({...base,createPdcaTest(input,user){if(!pdcaService||!user?.userId||!["owner","admin"].includes(user.role))throw new InternalTqmError("TQM_PDCA_GOVERNANCE_REQUIRED",403);if(!input||Object.keys(input).some(k=>!["comparison","pdcaPlan"].includes(k))||!input.comparison||!input.pdcaPlan)throw new InternalTqmError("TQM_COMPARISON_INVALID");const comparison=base.compare(input.comparison);if(!comparison.changeCandidate||comparison.stopLine)throw new InternalTqmError("TQM_CHANGE_CANDIDATE_NOT_TESTABLE",409);return pdcaService.create(input.pdcaPlan,user);}});}
