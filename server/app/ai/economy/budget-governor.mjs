export function createAiBudgetGovernor({softDailyTokenBudget=250000,softMonthlyTokenBudget=3000000,expertModelUsageLimit=25,warningThreshold=.8}={}){const usage=new Map();return Object.freeze({observe(workspaceId,route,tokens=0){const current=usage.get(workspaceId)||{tokens:0,terraCalls:0};current.tokens+=Math.max(0,tokens||0);if(route==="TERRA")current.terraCalls++;usage.set(workspaceId,current);return Object.freeze({warning:current.tokens>=softDailyTokenBudget*warningThreshold||current.terraCalls>=expertModelUsageLimit*warningThreshold,softLimitExceeded:current.tokens>=softDailyTokenBudget||current.terraCalls>=expertModelUsageLimit,blocking:false});},readiness:()=>Object.freeze({mode:"OBSERVE_AND_WARN",softDailyTokenBudget,softMonthlyTokenBudget,expertModelUsageLimit,warningThreshold})});}
