import{createRealtimeSubscription,canDeliverRealtime}from"./realtime-contract.mjs";export function createRealtimeRuntime({publish,subscribe}={}){if(typeof publish!=="function"||typeof subscribe!=="function")throw new Error("Realtime adapter requires publish and subscribe");return Object.freeze({async emit(event){if(!event?.workspaceId||!event?.appId||!event?.entityId)throw new Error("Realtime event scope is required");return publish(event);},async watch(input,onEvent){if(typeof onEvent!=="function")throw new Error("onEvent callback is required");const sub=createRealtimeSubscription(input);return subscribe(sub.channel,event=>{if(canDeliverRealtime(sub,event))onEvent(event);});}});}
