import{createNativeTarget}from"./native-renderer-contract.mjs";export function renderNativeManifest({definition,ui,versionId,platform,capabilities=[]}={}){if(!definition?.id||!ui?.views)throw new Error("definition and ui are required");const target=createNativeTarget({platform,appId:definition.id,versionId,capabilities});const screens=(ui.views||[]).filter(v=>!v.hidden).map(v=>({id:v.id,title:v.title||v.id,resource:v.resource||null,blocks:(v.blocks||[]).filter(b=>!b.hidden).map(b=>({type:b.type,props:b.props||{},fields:b.fields||null,columns:b.columns||null}))}));return Object.freeze({contract:"loadder.native.manifest.v1",target,app:{id:definition.id,name:definition.name,locale:definition.locale||"fa-IR",direction:ui.direction||"rtl"},screens,navigation:(ui.navigation||[]).filter(n=>screens.some(s=>s.id===n.id||n.route?.includes(s.id))),entities:definition.entities,workflows:definition.workflows,accessPolicy:definition.accessPolicy||null,ownership:definition.ownership});}
