import{createPaymentIntent,normalizePaymentEvent}from"./payment-contract.mjs";export function createPaymentRuntime({createIntent,verifyEvent,idempotencyStore}={}){if(typeof createIntent!=="function"||typeof verifyEvent!=="function"||!idempotencyStore||typeof idempotencyStore.has!=="function"||typeof idempotencyStore.add!=="function")throw new Error("Payment runtime adapter is incomplete");return Object.freeze({async start(input){const intent=createPaymentIntent(input),result=await createIntent(intent);if(!result?.providerId)throw new Error("Payment provider must return providerId");return{...intent,providerId:result.providerId,checkoutUrl:result.checkoutUrl||null};},async handleWebhook({payload,signature}){const raw=await verifyEvent({payload,signature});const event=normalizePaymentEvent(raw);if(await idempotencyStore.has(event.id))return{ok:true,idempotent:true,event};await idempotencyStore.add(event.id);return{ok:true,idempotent:false,event};}});}
export function createMemoryPaymentIdempotencyStore(){const seen=new Set();return{has:async id=>seen.has(id),add:async id=>{seen.add(id);return true}};}
