import express from"express";import{renderNativeManifest}from"./native-definition-renderer.mjs";export function createNativeRouter({projects}){const r=express.Router();r.get("/business-builder/projects/:id/native/:platform/manifest",(q,s)=>{const p=projects.getProject(q.params.id);if(!p?.activeVersionId)return s.status(404).json({success:false,code:"ACTIVE_VERSION_NOT_FOUND"});const v=p.versions.find(x=>x.id===p.activeVersionId);if(!v)return s.status(404).json({success:false,code:"ACTIVE_VERSION_NOT_FOUND"});try{const capabilities=String(q.query.capabilities||"").split(",").map(x=>x.trim()).filter(Boolean),manifest=renderNativeManifest({definition:v.definition,ui:v.ui,versionId:v.id,platform:q.params.platform,capabilities});return s.json({success:true,manifest});}catch(e){return s.status(400).json({success:false,code:"NATIVE_MANIFEST_INVALID",message:e?.message});}});return r;}
