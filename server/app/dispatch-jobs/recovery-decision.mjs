export function recommendDispatchJobRecovery({job,request,attempt=null,invocation=null,results=[],now=new Date(),reconciliationSupported=false}={}){if(!job||!request||job.completedAt||job.blockedReasonCode)return"NO_ACTION";const terminal=results.some(r=>["SUCCEEDED","FAILED","REJECTED_BEFORE_EXECUTION"].includes(r.resultType));if(terminal)return"COMPLETE_FROM_TERMINAL_RESULT";const unknown=results.some(r=>r.resultType==="UNKNOWN")||(attempt&&invocation&&Date.parse(now)>=Date.parse(attempt.timeoutAt));if(unknown)return reconciliationSupported?"RESCHEDULE_RECONCILIATION":"BLOCK_UNKNOWN";if(attempt&&!invocation)return"RESUME_PRE_INVOCATION_EVALUATION";if(attempt&&invocation)return"WAIT";return"NO_ACTION";}
