export class ShippingProviderError extends Error{constructor(code){super(code);this.code=code;}}
const result=(reference,tracking,status="READY")=>Object.freeze({providerShipmentReference:reference,trackingNumber:tracking,status});
export function createTestShippingProvider({mode="success",status="READY"}={}){return Object.freeze({provider:"TEST",version:1,configured:true,enabled:true,getCapabilities(){return Object.freeze({quote:false,create:true,track:true,cancel:false,pickupRequest:false,cod:false,insurance:false});},createShipment(input){if(mode==="unavailable"||mode==="timeout")throw new ShippingProviderError(mode==="timeout"?"SHIPPING_PROVIDER_OUTCOME_UNKNOWN":"SHIPPING_PROVIDER_UNAVAILABLE");if(mode==="invalid")return {raw:true};return result(`test:${input.fulfillmentId}`,`TEST-${input.fulfillmentId.slice(0,12)}`,status);},getShipmentStatus(input){if(mode==="unavailable")throw new ShippingProviderError("SHIPPING_PROVIDER_UNAVAILABLE");return Object.freeze({providerShipmentReference:input.providerShipmentReference,status,observedAt:"2026-08-23T12:00:00.000Z"});}});}
export function createUnavailableShippingProvider(provider="OTHER"){return Object.freeze({provider,version:1,configured:false,enabled:false,getCapabilities(){return Object.freeze({quote:false,create:false,track:false,cancel:false,pickupRequest:false,cod:false,insurance:false});},createShipment(){throw new ShippingProviderError("SHIPPING_NOT_CONFIGURED");},getShipmentStatus(){throw new ShippingProviderError("SHIPPING_NOT_CONFIGURED");}});}
