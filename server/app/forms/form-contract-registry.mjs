const types = new Set(["TEXT","TEXTAREA","EMAIL","PHONE","SELECT","RADIO","CHECKBOX","CONSENT","NUMBER","DATE"]), pii = new Set(["NON_PII","CONTACT_PII"]), purposes = new Set(["LEAD","CONTACT","QUOTE_REQUEST","APPOINTMENT_REQUEST","CALLBACK_REQUEST","NEWSLETTER"]);
export const formFieldRegistry = Object.freeze({ version: 1, types: Object.freeze([...types]), purposes: Object.freeze([...purposes]), validate(definition, purpose) { if (!purposes.has(purpose) || !definition || Object.keys(definition).some((key) => !["fields"].includes(key)) || !Array.isArray(definition.fields) || definition.fields.length < 1 || definition.fields.length > 20) return false; const keys = new Set(); let consent = false; for (const [index, field] of definition.fields.entries()) { if (!field || Object.keys(field).some((key) => !["key","label","type","required","placeholder","helpText","options","piiClassification","order"].includes(key)) || !/^[a-z][a-z0-9_]{0,39}$/.test(field.key) || keys.has(field.key) || typeof field.label !== "string" || field.label.trim().length < 1 || field.label.length > 100 || !types.has(field.type) || typeof field.required !== "boolean" || !pii.has(field.piiClassification) || field.order !== index || (field.placeholder != null && (typeof field.placeholder !== "string" || field.placeholder.length > 160)) || (field.helpText != null && (typeof field.helpText !== "string" || field.helpText.length > 300))) return false; keys.add(field.key); if (["SELECT","RADIO"].includes(field.type)) { if (!Array.isArray(field.options) || field.options.length < 1 || field.options.length > 20 || field.options.some((option) => typeof option !== "string" || !option.trim() || option.length > 100) || new Set(field.options).size !== field.options.length) return false; } else if (field.options != null) return false; if (field.type === "CONSENT") consent = field.required; if (["EMAIL","PHONE"].includes(field.type) && field.piiClassification !== "CONTACT_PII") return false; } return purpose !== "NEWSLETTER" || consent; } });
