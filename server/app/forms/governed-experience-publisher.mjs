import crypto from"node:crypto";import{mkdirSync,readFileSync,unlinkSync,writeFileSync}from"node:fs";import{dirname,join}from"node:path";import{createLandingPublisher}from"../landing/landing-publisher.mjs";
const esc=v=>String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c]),safe=v=>v.replace(/[^a-z0-9/_-]/g,"-"),formSections=b=>b.blueprint.sections.filter(s=>s.componentId==="FORM_OR_ACTION"&&s.variant==="FORM_DEFINITION"&&typeof s.props?.publicFormReference==="string"&&Number.isInteger(s.props?.formRevision));
function render({blueprint,apiOrigin}){const b=blueprint.blueprint,forms=formSections(blueprint),sections=b.sections.map((s,index)=>`<section data-component="${esc(s.componentId)}"><${index?"h2":"h1"}>${esc(s.props.heading||s.props.headline||s.componentId)}</${index?"h2":"h1"}>${s.props.body?`<p>${esc(s.props.body)}</p>`:""}${s.componentId==="FORM_OR_ACTION"&&s.variant==="FORM_DEFINITION"?`<div class="loadder-form" data-form-reference="${esc(s.props.publicFormReference)}" data-form-revision="${s.props.formRevision}"></div>`:""}</section>`).join(""),client=`(()=>{const api=${JSON.stringify(apiOrigin)},token=new URLSearchParams(location.search).get('lt'),anon=localStorage.getItem('ld_form_anon')||crypto.randomUUID().replaceAll('-','');localStorage.setItem('ld_form_anon',anon);const element=(tag,text)=>{const e=document.createElement(tag);if(text)e.textContent=text;return e};for(const root of document.querySelectorAll('.loadder-form')){fetch(api+'/api/public/forms/'+encodeURIComponent(root.dataset.formReference)).then(r=>r.ok?r.json():Promise.reject()).then(({form})=>{if(String(form.revision)!==root.dataset.formRevision)throw Error();const node=element('form');node.noValidate=true;const error=element('p');error.className='form-error';error.setAttribute('role','alert');for(const field of [...form.fields].sort((a,b)=>a.order-b.order)){const id='f_'+root.dataset.formReference+'_'+field.key,label=element('label',field.label+(field.required?' *':''));label.htmlFor=id;let input;if(field.type==='TEXTAREA')input=element('textarea');else if(field.type==='SELECT')input=element('select');else{input=element('input');input.type=field.type==='EMAIL'?'email':field.type==='PHONE'?'tel':field.type==='NUMBER'?'number':field.type==='DATE'?'date':field.type==='CHECKBOX'||field.type==='CONSENT'?'checkbox':'text'}input.id=id;input.name=field.key;input.required=field.required;input.placeholder=field.placeholder||'';input.setAttribute('aria-required',String(field.required));for(const option of field.options||[]){const o=element('option',option);o.value=option;input.append(o)}label.append(input);if(field.helpText){const help=element('small',field.helpText);help.id=id+'_help';input.setAttribute('aria-describedby',help.id);label.append(help)}node.append(label)}const hp=element('input');hp.name='company_website';hp.tabIndex=-1;hp.autocomplete='off';hp.className='hp';node.append(hp,error);const button=element('button','ارسال درخواست');button.type='submit';node.append(button);let startedAt=new Date().toISOString();node.addEventListener('submit',async event=>{event.preventDefault();error.textContent='';const invalid=node.querySelector(':invalid');if(invalid){error.textContent='لطفاً فیلدهای ضروری را درست تکمیل کنید.';invalid.focus();return}button.disabled=true;button.textContent='در حال ارسال…';const values={};for(const field of form.fields){const input=node.elements.namedItem(field.key);values[field.key]=input.type==='checkbox'?input.checked:input.type==='number'&&input.value!==''?Number(input.value):input.value}const idem=crypto.randomUUID();try{const response=await fetch(api+'/api/public/forms/'+encodeURIComponent(root.dataset.formReference)+'/submissions',{method:'POST',headers:{'content-type':'application/json','Idempotency-Key':idem},body:JSON.stringify({values,trackingToken:token||undefined,anonymousId:anon,honeypot:hp.value,startedAt})}),body=await response.json();if(!response.ok){error.textContent=body.code==='FORM_FIELD_INVALID'?'اطلاعات فرم را بررسی کنید.':'ثبت درخواست انجام نشد.';button.disabled=false;button.textContent='ارسال درخواست';return}if(body.successBehavior==='SAFE_REDIRECT'&&typeof body.successValue==='string'&&body.successValue.startsWith('/')&&!body.successValue.startsWith('//'))location.assign(body.successValue);else root.replaceChildren(element('p',body.successValue||'درخواست شما دریافت شد.'))}catch{error.textContent='ارتباط برقرار نشد. دوباره تلاش کنید.';button.disabled=false;button.textContent='ارسال درخواست'}});root.append(node)}).catch(()=>{root.textContent='این فرم اکنون در دسترس نیست.'})}})();`,scriptHash=crypto.createHash("sha256").update(client).digest("base64"),style=`*{box-sizing:border-box}body{margin:0;background:${b.designTokens.backgroundColor};color:${b.designTokens.foregroundColor};font-family:system-ui,sans-serif}main{max-width:64rem;margin:auto;padding:1rem}section{padding:3rem 0}p,small{line-height:1.8;color:${b.designTokens.mutedColor}}form{display:grid;gap:1rem;max-width:40rem}label{display:grid;gap:.4rem;font-weight:600}input,textarea,select,button{font:inherit;min-height:48px;border-radius:12px;padding:.75rem}input,textarea,select{border:1px solid ${b.designTokens.mutedColor};background:${b.designTokens.backgroundColor};color:${b.designTokens.foregroundColor}}textarea{min-height:8rem}button{border:0;background:${b.designTokens.primaryColor};color:#fff;font-weight:700;touch-action:manipulation}.form-error{color:#fca5a5}.hp{position:absolute!important;left:-10000px!important;width:1px!important;height:1px!important} :focus-visible{outline:3px solid ${b.designTokens.primaryColor};outline-offset:3px}@media(max-width:640px){section{padding:2rem 0}input,textarea,select,button{width:100%}}`,html=`<!doctype html><html lang="${esc(blueprint.locale)}" dir="${esc(blueprint.direction)}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(b.seo.title)}</title><meta name="description" content="${esc(b.seo.description)}"><meta property="og:title" content="${esc(b.socialPreview.title)}"><meta property="og:description" content="${esc(b.socialPreview.description)}"><style>${style}</style></head><body><main aria-label="${esc(b.accessibility.mainLabel)}">${sections}</main><script>${client}</script></body></html>`,checksum=crypto.createHash("sha256").update(html).digest("hex"),csp=`default-src 'none';script-src 'sha256-${scriptHash}';style-src 'unsafe-inline';connect-src ${apiOrigin};base-uri 'none';form-action 'none';frame-ancestors 'none'`;return{html,checksum,csp,forms};}
export function createGovernedExperiencePublisher(options={}){const delegate=createLandingPublisher(options),production=options.nodeEnv==="production",base=String(options.publicBaseUrl||"").replace(/\/$/,""),api=String(options.publicApiBaseUrl||base||"").replace(/\/$/,""),memory=new Map(),configured=!production||Boolean(options.staticDirectory&&/^https:\/\//.test(base)&&/^https:\/\//.test(api)),persist=(path,html)=>{memory.set(path,html);if(options.staticDirectory){const target=join(options.staticDirectory,path);mkdirSync(dirname(target),{recursive:true});writeFileSync(target,html,{encoding:"utf8",mode:0o644});}};return Object.freeze({...delegate,configured,publishRegisteredBlueprint(input){if(!formSections(input.blueprint).length)return delegate.publishRegisteredBlueprint(input);if(!configured)return Object.freeze({available:false,failureCode:"LANDING_PUBLISHER_NOT_CONFIGURED"});const artifact=render({blueprint:input.blueprint,apiOrigin:api}),path=safe(`experiences/${input.project.slug}/v${input.blueprint.version}-${input.blueprint.contentHash.slice(0,12)}.html`);persist(path,artifact.html);return Object.freeze({available:true,host:base||"local",path,artifactChecksum:artifact.checksum});},resolvePublication(input){if(!formSections(input.blueprint).length)return delegate.resolvePublication(input);const artifact=render({blueprint:input.blueprint,apiOrigin:api});let html=memory.get(input.publication.path);if(!html&&options.staticDirectory)try{html=readFileSync(join(options.staticDirectory,input.publication.path),"utf8");}catch{}return html&&artifact.checksum===input.publication.artifactChecksum?{html,csp:artifact.csp}:null;},removePublication(publication){delegate.removePublication(publication);memory.delete(publication.path);if(options.staticDirectory)try{unlinkSync(join(options.staticDirectory,publication.path));}catch{}}});}
