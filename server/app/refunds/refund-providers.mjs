export class RefundProviderError extends Error{constructor(code){super(code);this.code=code;}}
export function createTestRefundProvider({submission="SUCCESS",verification="VERIFIED",amountDelta=0,currencyOverride=null,malformed=false}={}){return Object.freeze({provider:"TEST",version:1,configured:true,enabled:true,supportsRefunds:true,createRefund(x){if(submission==="UNAVAILABLE")throw new RefundProviderError("REFUND_PROVIDER_UNAVAILABLE");if(submission==="FAILED")return{status:"FAILED",providerRefundReference:null,submittedAt:null};return{status:"SUBMITTED",providerRefundReference:`refund:${x.attemptId}`,submittedAt:new Date("2026-08-23T12:00:00.000Z").toISOString()};},verifyRefund(x){if(malformed)return{raw:true};if(verification==="UNAVAILABLE")throw new RefundProviderError("REFUND_PROVIDER_UNAVAILABLE");if(verification!=="VERIFIED")return{status:verification,verifiedAmount:null,currency:null,providerRefundReference:null,verifiedAt:null,boundedProviderCode:verification};return{status:"VERIFIED",verifiedAmount:x.expectedAmount+amountDelta,currency:currencyOverride||x.currency,providerRefundReference:x.providerRefundReference,verifiedAt:new Date("2026-08-23T12:00:00.000Z").toISOString(),boundedProviderCode:"VERIFIED"};}});}
export function createUnavailableRefundProvider(){return Object.freeze({provider:"OTHER",version:1,configured:false,enabled:false,supportsRefunds:false,createRefund(){throw new RefundProviderError("REFUND_PROVIDER_UNSUPPORTED");},verifyRefund(){throw new RefundProviderError("REFUND_PROVIDER_UNSUPPORTED");}});}
