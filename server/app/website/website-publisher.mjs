import{landingHash}from"../landing/landing-contracts.mjs";
export function createWebsitePublisher({landingPublisher}){return Object.freeze({configured:landingPublisher.configured,publish({project,pages}){if(!landingPublisher.configured)return Object.freeze({available:false,failureCode:"WEBSITE_PUBLISHER_NOT_CONFIGURED"});const build=navigation=>pages.map(({page,blueprint})=>{const artifact=landingPublisher.publishRegisteredBlueprint({project:{slug:`website-${project.slug}-${page.path.replaceAll("/","-")||"home"}`},blueprint,navigation});if(!artifact.available)throw Error("WEBSITE_PUBLISHER_NOT_CONFIGURED");return{pageId:page.id,name:page.name,path:page.path,blueprintId:blueprint.id,blueprintVersion:blueprint.version,rendererVersion:blueprint.rendererVersion,componentRegistryVersion:blueprint.componentRegistryVersion,artifactPath:artifact.path,artifactChecksum:artifact.artifactChecksum,host:artifact.host};}),initial=build([]),navigation=initial.map(x=>({label:x.name,href:`/${x.artifactPath}`})),rendered=build(navigation),artifacts=rendered.map(({host:_host,...x})=>Object.freeze(x)),manifest=Object.freeze({schemaVersion:1,websiteProjectId:project.id,siteSlug:project.slug,themeVersion:1,pages:Object.freeze(artifacts)});return Object.freeze({available:true,host:rendered[0]?.host||"local",basePath:`websites/${project.slug}`,manifest,manifestHash:landingHash(manifest)});},publicUrl(publication){const home=publication.manifest.pages.find(x=>x.path==="/")||publication.manifest.pages[0],path=home?.artifactPath||publication.basePath;return publication.host&&publication.host!=="local"?`${publication.host}/${path}`:`/${path}`;}});}
