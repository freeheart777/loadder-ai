import crypto from"node:crypto";
const canonical=value=>value===null||typeof value!=="object"?JSON.stringify(value):Array.isArray(value)?`[${value.map(canonical).join(",")}]`:`{${Object.keys(value).sort().map(k=>`${JSON.stringify(k)}:${canonical(value[k])}`).join(",")}}`;
export function buildProviderIdempotencyKey(input){const fields=["workspaceId","executionRequestId","requestFingerprint","capabilityId","capabilityVersion","providerAccountIdentityId"];if(!input||typeof input!=="object"||Array.isArray(input)||Object.keys(input).some(k=>!fields.includes(k))||fields.some(k=>input[k]===undefined)||!Number.isInteger(input.capabilityVersion)||input.capabilityVersion<1)throw new Error("Provider idempotency identity is invalid.");return crypto.createHash("sha256").update(canonical(Object.fromEntries(fields.map(k=>[k,input[k]])))).digest("hex");}
