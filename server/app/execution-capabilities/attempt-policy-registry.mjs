const FIELDS=new Set(["policyId","version","capabilityId","capabilityVersion","maxAttempts","timeoutMs","providerIdempotencyRequired","reconciliationAfterUnknownRequired","automaticRetryAllowed","manualRetryOnly","retryableDefinitiveFailureCodes"]),ID=/^[a-z][a-z0-9_.-]{0,99}$/;
function validate(entry){if(!entry||typeof entry!=="object"||Array.isArray(entry)||Object.keys(entry).some(k=>!FIELDS.has(k))||![entry.policyId,entry.capabilityId].every(v=>typeof v==="string"&&ID.test(v))||![entry.version,entry.capabilityVersion,entry.maxAttempts,entry.timeoutMs].every(v=>Number.isInteger(v)&&v>0)||entry.maxAttempts>10||!["providerIdempotencyRequired","reconciliationAfterUnknownRequired","automaticRetryAllowed","manualRetryOnly"].every(k=>typeof entry[k]==="boolean")||!Array.isArray(entry.retryableDefinitiveFailureCodes)||entry.automaticRetryAllowed||!entry.manualRetryOnly)throw new Error("Attempt policy is invalid.");return Object.freeze({...entry,retryableDefinitiveFailureCodes:Object.freeze([...entry.retryableDefinitiveFailureCodes])});}
export function createAttemptPolicyRegistry(entries=[]){const policies=entries.map(validate);return Object.freeze({list:()=>[...policies],get:(id,version=1)=>policies.find(p=>p.policyId===id&&p.version===version)||null});}
// Retry execution is not available.
export const attemptPolicyRegistry=createAttemptPolicyRegistry();
