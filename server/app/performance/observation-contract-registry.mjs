const TYPES=Object.freeze({SEARCH_OBSERVATION_V1:["IMPRESSION","CLICK","LEAD","PURCHASE"],SOCIAL_OBSERVATION_V1:["VIEW","ENGAGEMENT","PROFILE_VISIT","MESSAGE"],MESSAGING_OBSERVATION_V1:["SENT","DELIVERED","REPLY"],MARKETPLACE_OBSERVATION_V1:["PRODUCT_VIEW","CLICK_OUT","PURCHASE"],OWNED_WEB_OBSERVATION_V1:["PAGE_VIEW","CTA_CLICK","FORM_SUBMIT","BOOKING"],OWNED_WEB_OBSERVATION_V2:["PAGE_VIEW","CTA_CLICK","FORM_START","FORM_SUBMIT","BOOKING"]});
const defs=Object.entries(TYPES).map(([observationContractId,allowedObservationTypes])=>({observationContractId,observationContractVersion:observationContractId.endsWith("V2")?2:1,allowedObservationTypes,valueSchema:"NON_NEGATIVE_INTEGER",allowedUnits:["COUNT"],requiredProvenance:["externalReference","payloadFingerprint"]}));
export function createObservationContractRegistry(entries=defs){const registry=new Map();for(const entry of entries){if(!entry||typeof entry.observationContractId!=="string"||!Number.isInteger(entry.observationContractVersion)||entry.observationContractVersion<1||!Array.isArray(entry.allowedObservationTypes)||!entry.allowedObservationTypes.length||entry.valueSchema!=="NON_NEGATIVE_INTEGER"||!Array.isArray(entry.allowedUnits)||!entry.allowedUnits.length||!Array.isArray(entry.requiredProvenance))throw new Error("Invalid observation contract.");const key=`${entry.observationContractId}@${entry.observationContractVersion}`;if(registry.has(key))throw new Error("Duplicate observation contract.");registry.set(key,Object.freeze({...entry,allowedObservationTypes:Object.freeze([...entry.allowedObservationTypes]),allowedUnits:Object.freeze([...entry.allowedUnits]),requiredProvenance:Object.freeze([...entry.requiredProvenance])}));}return Object.freeze({get:(id,version)=>registry.get(`${id}@${version}`)||null,list:()=>[...registry.values()]});}
export const observationContractRegistry=createObservationContractRegistry();
