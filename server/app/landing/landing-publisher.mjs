import crypto from"node:crypto";const esc=v=>String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
export function createLandingPublisher({nodeEnv="development"}={}){return Object.freeze({publishRegisteredBlueprint({project,blueprint}){if(nodeEnv==="production")return Object.freeze({available:false,failureCode:"LANDING_PUBLISHER_NOT_CONFIGURED"});const b=blueprint.blueprint,sections=b.sections.map(s=>`<section data-component="${esc(s.componentId)}" data-variant="${esc(s.variant)}"><h2>${esc(s.props.heading||s.props.headline||s.componentId)}</h2>${s.props.body?`<p>${esc(s.props.body)}</p>`:""}</section>`).join("");const artifact=`<!doctype html><html lang="${esc(blueprint.locale)}" dir="${esc(blueprint.direction)}"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(b.seo.title)}</title><meta name="description" content="${esc(b.seo.description)}"></head><body><main aria-label="${esc(b.accessibility.mainLabel)}">${sections}</main></body></html>`,checksum=crypto.createHash("sha256").update(artifact).digest("hex");return Object.freeze({available:true,host:"local",path:`/landing-artifacts/${project.slug}/v${blueprint.version}.html`,artifactChecksum:checksum,artifact});}});}
