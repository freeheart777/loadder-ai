import express from "express";
import { CreativeIntentError } from "../services/creative-intent-service.mjs";
export function createCreativeIntentRouter({service}){const router=express.Router(),actor=req=>({userId:req.user.id,role:req.membership.role}),handle=(error,res)=>error instanceof CreativeIntentError?res.status(error.status).json({success:false,code:error.code,message:"Creative intent operation could not be completed."}):res.status(500).json({success:false,code:"CREATIVE_INTENT_INVALID",message:"Creative intent operation could not be completed."});router.post("/creative-intents",(req,res)=>{try{const result=service.create(req.body,actor(req),req.headers["idempotency-key"]);return res.status(result.reusedResult?200:201).json({success:true,...result});}catch(error){return handle(error,res);}});router.get("/creative-intents",(req,res)=>{try{return res.json({success:true,...service.list(req.query,actor(req))});}catch(error){return handle(error,res);}});return router;}
