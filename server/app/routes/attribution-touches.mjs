import express from "express";
import {AttributionTouchError} from "../services/attribution-touch-service.mjs";
export function createAttributionTouchRouter({service}){const router=express.Router(),actor=req=>({userId:req.user.id,role:req.membership.role}),handle=(error,res)=>error instanceof AttributionTouchError?res.status(error.status).json({success:false,code:error.code,message:"Attribution touch operation could not be completed."}):res.status(500).json({success:false,code:"ATTRIBUTION_TOUCH_INVALID",message:"Attribution touch operation could not be completed."});router.post("/attribution/touches",(req,res)=>{try{const result=service.create(req.body,actor(req),req.headers["idempotency-key"]);return res.status(result.reusedResult?200:201).json({success:true,...result});}catch(error){return handle(error,res);}});router.get("/distribution-contexts/:id/touches",(req,res)=>{try{return res.json({success:true,...service.list(req.params.id,req.query,actor(req))});}catch(error){return handle(error,res);}});return router;}
