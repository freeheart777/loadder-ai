import express from"express";import{ForecastError}from"../services/forecast-service.mjs";
export function createForecastRouter({service}){const r=express.Router(),h=(e,res)=>e instanceof ForecastError?res.status(e.status).json({success:false,code:e.code,message:e.message}):res.status(500).json({success:false,message:"Unable to process Forecast."});r.get("/forecast-specifications",(q,s)=>s.json({success:true,specifications:service.listSpecifications()}));r.post("/forecasts/run",(q,s)=>{try{return s.status(201).json({success:true,forecast:service.run(q.body||{})});}catch(e){return h(e,s);}});r.get("/forecasts",(q,s)=>{try{return s.json({success:true,forecasts:service.list(q.query)});}catch(e){return h(e,s);}});r.get("/forecasts/:id",(q,s)=>{try{return s.json({success:true,forecast:service.get(q.params.id)});}catch(e){return h(e,s);}});return r;}
