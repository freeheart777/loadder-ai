import express from"express";import{InternalTqmError}from"../services/internal-tqm-service.mjs";
export function createInternalTqmRouter({service}){const r=express.Router(),run=(s,fn)=>{try{return s.json({success:true,...fn()});}catch(e){return s.status(e instanceof InternalTqmError?e.status:500).json({success:false,code:e instanceof InternalTqmError?e.code:"INTERNAL_TQM_ERROR"});}};r.get("/internal/quality/summary",(_q,s)=>run(s,()=>service.summary()));r.post("/internal/quality/compare",(q,s)=>run(s,()=>({comparison:service.compare(q.body)})));r.get("/internal/quality/constraints",(_q,s)=>run(s,()=>service.constraints()));return r;}
