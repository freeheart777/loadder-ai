import express from"express";import{WorkflowOutcomeError}from"../services/workflow-outcome-service.mjs";
export function createWorkflowOutcomeRouter({service}){const r=express.Router(),run=(s,fn)=>{try{return s.json({success:true,...fn()});}catch(e){return s.status(e instanceof WorkflowOutcomeError?e.status:500).json({success:false,code:e instanceof WorkflowOutcomeError?e.code:"OUTCOME_MEASUREMENT_ERROR"});}},input=q=>q.query;r.get("/internal/quality/user-outcomes/registry",(_q,s)=>run(s,()=>service.registry()));r.get("/internal/quality/user-outcomes/summary",(q,s)=>run(s,()=>service.summary(input(q))));r.get("/internal/quality/user-outcomes/:workflowType",(q,s)=>run(s,()=>service.instances(q.params.workflowType,input(q))));return r;}
