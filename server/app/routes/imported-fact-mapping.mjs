import express from"express";import{FactMappingError}from"../services/imported-fact-event-mapper-service.mjs";export function createImportedFactMappingRouter({service}){const r=express.Router(),h=(e,s)=>e instanceof FactMappingError?s.status(e.status).json({success:false,code:e.code,message:e.message}):s.status(500).json({success:false,message:"Imported Fact mapping failed."});r.get("/import-mappers",(q,s)=>s.json({success:true,mappers:service.definitions()}));r.post("/imported-facts/:id/map-event",(q,s)=>{try{return s.status(201).json({success:true,...service.map(q.params.id,q.user.id)});}catch(e){return h(e,s);}});return r;}
