import express from "express";
import { CreativePlacementError } from "../services/creative-placement-service.mjs";
export function createCreativePlacementRouter({service}){const router=express.Router(),actor=req=>({userId:req.user.id,role:req.membership.role}),handle=(error,res)=>error instanceof CreativePlacementError?res.status(error.status).json({success:false,code:error.code,message:"Creative placement operation could not be completed."}):res.status(500).json({success:false,code:"CREATIVE_PLACEMENT_INVALID",message:"Creative placement operation could not be completed."});router.post("/content/items/:id/placements",(req,res)=>{try{const result=service.create(req.params.id,req.body,actor(req),req.headers["idempotency-key"]);return res.status(result.reusedResult?200:201).json({success:true,placement:result.placement,reusedResult:result.reusedResult});}catch(error){return handle(error,res);}});router.get("/content/items/:id/placements",(req,res)=>{try{const page=service.list(req.params.id,req.query,actor(req));return res.json({success:true,...page});}catch(error){return handle(error,res);}});return router;}
