import express from "express";
import {DistributionContextError} from "../services/distribution-context-service.mjs";
export function createDistributionContextRouter({service}){const router=express.Router(),actor=req=>({userId:req.user.id,role:req.membership.role}),handle=(error,res)=>error instanceof DistributionContextError?res.status(error.status).json({success:false,code:error.code,message:"Distribution context operation could not be completed."}):res.status(500).json({success:false,code:"DISTRIBUTION_CONTEXT_INVALID",message:"Distribution context operation could not be completed."});router.post("/creative-placements/:id/distribution-contexts",(req,res)=>{try{const result=service.create(req.params.id,req.body,actor(req),req.headers["idempotency-key"]);return res.status(result.reusedResult?200:201).json({success:true,...result});}catch(error){return handle(error,res);}});router.get("/creative-placements/:id/distribution-contexts",(req,res)=>{try{return res.json({success:true,...service.list(req.params.id,req.query,actor(req))});}catch(error){return handle(error,res);}});return router;}
