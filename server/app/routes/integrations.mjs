import express from"express";import{IntegrationError}from"../services/integration-service.mjs";export function createIntegrationRouter({service}){const r=express.Router(),h=(e,s)=>e instanceof IntegrationError?s.status(e.status).json({success:false,code:e.code,message:e.message}):s.status(500).json({success:false,message:"Integration operation failed."});r.get("/connector-definitions",(q,s)=>{try{return s.json({success:true,connectors:service.definitions(q.query.region)});}catch(e){return h(e,s);}});r.post("/connections",(q,s)=>{try{return s.status(201).json({success:true,connection:service.createConnection(q.body||{},q.user.id)});}catch(e){return h(e,s);}});r.get("/connections",(q,s)=>s.json({success:true,connections:service.connections()}));r.get("/connections/:id",(q,s)=>{try{return s.json({success:true,connection:service.connection(q.params.id)});}catch(e){return h(e,s);}});r.post("/imports",(q,s)=>{try{return s.status(201).json({success:true,...service.import(q.body||{},q.user.id)});}catch(e){return h(e,s);}});r.get("/imports",(q,s)=>s.json({success:true,batches:service.batches()}));r.get("/imports/:id",(q,s)=>{try{return s.json({success:true,...service.batch(q.params.id)});}catch(e){return h(e,s);}});return r;}
