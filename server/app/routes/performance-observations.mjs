import express from"express";import{PerformanceObservationError}from"../services/performance-observation-service.mjs";
export function createPerformanceObservationRouter({service}){const router=express.Router(),actor=req=>({userId:req.user.id,role:req.membership.role}),handle=(e,res)=>e instanceof PerformanceObservationError?res.status(e.status).json({success:false,code:e.code,message:"Performance observation operation could not be completed."}):res.status(500).json({success:false,code:"PERFORMANCE_OBSERVATION_INVALID",message:"Performance observation operation could not be completed."});router.post("/performance/observations",(req,res)=>{try{const result=service.create(req.body,actor(req),req.headers["idempotency-key"]);return res.status(result.reusedResult?200:201).json({success:true,...result});}catch(e){return handle(e,res);}});router.get("/distribution-contexts/:id/observations",(req,res)=>{try{return res.json({success:true,...service.list(req.params.id,req.query,actor(req))});}catch(e){return handle(e,res);}});return router;}
