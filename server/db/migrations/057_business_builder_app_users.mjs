export const migration057BusinessBuilderAppUsers={id:57,name:"business_builder_app_users",up(db){db.exec(`CREATE TABLE IF NOT EXISTS business_builder_app_users(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL,project_id TEXT NOT NULL,email TEXT NOT NULL,display_name TEXT,role TEXT NOT NULL CHECK(role IN ('customer','employee','manager','admin')),status TEXT NOT NULL DEFAULT 'active',created_at TEXT NOT NULL,updated_at TEXT NOT NULL,UNIQUE(workspace_id,project_id,email),FOREIGN KEY(workspace_id) REFERENCES workspaces(id) ON DELETE CASCADE,FOREIGN KEY(project_id) REFERENCES business_builder_projects(id) ON DELETE CASCADE);CREATE TABLE IF NOT EXISTS business_builder_app_sessions(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL,project_id TEXT NOT NULL,app_user_id TEXT NOT NULL,token_hash TEXT NOT NULL UNIQUE,expires_at TEXT NOT NULL,created_at TEXT NOT NULL,revoked_at TEXT,FOREIGN KEY(app_user_id) REFERENCES business_builder_app_users(id) ON DELETE CASCADE);CREATE INDEX IF NOT EXISTS idx_bb_app_users_project ON business_builder_app_users(workspace_id,project_id,status);CREATE INDEX IF NOT EXISTS idx_bb_app_sessions_lookup ON business_builder_app_sessions(token_hash,expires_at);`);}};
