export const migration062BusinessBuilderAppStoreInstallations={version:62,name:"business_builder_app_store_installations",up(db){db.exec(`CREATE TABLE IF NOT EXISTS business_builder_app_installations(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL,project_id TEXT NOT NULL,blueprint_id TEXT NOT NULL,installed_version TEXT NOT NULL,available_version TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'current',installed_at TEXT NOT NULL,updated_at TEXT NOT NULL,UNIQUE(workspace_id,project_id),FOREIGN KEY(workspace_id) REFERENCES workspaces(id) ON DELETE CASCADE,FOREIGN KEY(project_id) REFERENCES business_builder_projects(id) ON DELETE CASCADE);CREATE INDEX IF NOT EXISTS idx_bb_app_installations_blueprint ON business_builder_app_installations(workspace_id,blueprint_id,status);`);}};