export const migration063ReturnRefundLifecycle={version:63,name:"return_refund_lifecycle",up(db){db.exec(`
CREATE TABLE IF NOT EXISTS commerce_return_requests(id TEXT PRIMARY KEY,public_reference TEXT NOT NULL,workspace_id TEXT NOT NULL REFERENCES workspaces(id),confirmed_order_id TEXT NOT NULL REFERENCES commerce_orders(id),status TEXT NOT NULL CHECK(status IN('REQUESTED','UNDER_REVIEW','APPROVED','REJECTED','AWAITING_RETURN','RECEIVED','RESOLVED','CANCELLED')),reason_code TEXT NOT NULL CHECK(reason_code IN('DAMAGED','WRONG_ITEM','NOT_AS_DESCRIBED','SIZE_OR_FIT','CHANGED_MIND','QUALITY_ISSUE','INVENTORY_UNAVAILABLE','OTHER')),reason_note TEXT CHECK(reason_note IS NULL OR length(reason_note)<=1000),customer_scope_hash TEXT NOT NULL CHECK(length(customer_scope_hash)=64),requested_at TEXT NOT NULL,reviewed_at TEXT,resolved_at TEXT,reviewed_by_user_id TEXT REFERENCES users(id),review_reason_code TEXT,restock_completed_at TEXT,idempotency_key TEXT NOT NULL,request_hash TEXT NOT NULL CHECK(length(request_hash)=64),created_at TEXT NOT NULL,updated_at TEXT NOT NULL);
CREATE UNIQUE INDEX IF NOT EXISTS uq_return_public ON commerce_return_requests(public_reference);CREATE UNIQUE INDEX IF NOT EXISTS uq_return_customer_key ON commerce_return_requests(workspace_id,confirmed_order_id,customer_scope_hash,idempotency_key);CREATE INDEX IF NOT EXISTS idx_return_admin ON commerce_return_requests(workspace_id,status,updated_at DESC,id);CREATE INDEX IF NOT EXISTS idx_return_customer ON commerce_return_requests(workspace_id,confirmed_order_id,customer_scope_hash,created_at DESC,id);
CREATE TABLE IF NOT EXISTS commerce_return_items(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL REFERENCES workspaces(id),return_request_id TEXT NOT NULL REFERENCES commerce_return_requests(id),confirmed_order_item_id TEXT NOT NULL REFERENCES commerce_order_items(id),quantity_requested INTEGER NOT NULL CHECK(quantity_requested>0),quantity_approved INTEGER CHECK(quantity_approved IS NULL OR quantity_approved BETWEEN 0 AND quantity_requested),quantity_received INTEGER CHECK(quantity_received IS NULL OR quantity_received BETWEEN 0 AND COALESCE(quantity_approved,quantity_requested)),restock_mode TEXT CHECK(restock_mode IS NULL OR restock_mode IN('RESTOCK','DO_NOT_RESTOCK')),restocked_at TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL);
CREATE UNIQUE INDEX IF NOT EXISTS uq_return_line ON commerce_return_items(workspace_id,return_request_id,confirmed_order_item_id);CREATE INDEX IF NOT EXISTS idx_return_item_order ON commerce_return_items(workspace_id,confirmed_order_item_id,return_request_id);
CREATE TABLE IF NOT EXISTS commerce_refund_attempts(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL REFERENCES workspaces(id),confirmed_order_id TEXT NOT NULL REFERENCES commerce_orders(id),verified_payment_id TEXT NOT NULL REFERENCES commerce_verified_payments(id),return_request_id TEXT REFERENCES commerce_return_requests(id),provider TEXT NOT NULL CHECK(length(provider) BETWEEN 1 AND 40),provider_binding_version INTEGER NOT NULL CHECK(provider_binding_version>0),status TEXT NOT NULL CHECK(status IN('CREATED','SUBMISSION_PENDING','SUBMITTED','VERIFYING','VERIFIED','FAILED','PENDING','CANCELLED')),requested_amount INTEGER NOT NULL CHECK(requested_amount>0),currency TEXT NOT NULL,idempotency_key TEXT NOT NULL,request_hash TEXT NOT NULL CHECK(length(request_hash)=64),provider_refund_reference TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL,verification_started_at TEXT,verified_at TEXT,failed_at TEXT,failure_code TEXT);
CREATE UNIQUE INDEX IF NOT EXISTS uq_refund_attempt_key ON commerce_refund_attempts(workspace_id,confirmed_order_id,idempotency_key);CREATE UNIQUE INDEX IF NOT EXISTS uq_refund_provider_reference ON commerce_refund_attempts(provider,provider_refund_reference) WHERE provider_refund_reference IS NOT NULL;CREATE INDEX IF NOT EXISTS idx_refund_order ON commerce_refund_attempts(workspace_id,confirmed_order_id,created_at DESC,id);CREATE INDEX IF NOT EXISTS idx_refund_state ON commerce_refund_attempts(workspace_id,status,updated_at,id);
CREATE TABLE IF NOT EXISTS commerce_verified_refunds(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL REFERENCES workspaces(id),refund_attempt_id TEXT NOT NULL REFERENCES commerce_refund_attempts(id),verified_payment_id TEXT NOT NULL REFERENCES commerce_verified_payments(id),confirmed_order_id TEXT NOT NULL REFERENCES commerce_orders(id),return_request_id TEXT REFERENCES commerce_return_requests(id),provider TEXT NOT NULL,provider_refund_reference TEXT NOT NULL,verified_amount INTEGER NOT NULL CHECK(verified_amount>0),currency TEXT NOT NULL,verified_at TEXT NOT NULL,verification_method TEXT NOT NULL,verification_version INTEGER NOT NULL CHECK(verification_version>0),provider_result_code TEXT,created_at TEXT NOT NULL);
CREATE UNIQUE INDEX IF NOT EXISTS uq_verified_refund_attempt ON commerce_verified_refunds(workspace_id,refund_attempt_id);CREATE UNIQUE INDEX IF NOT EXISTS uq_verified_refund_provider ON commerce_verified_refunds(provider,provider_refund_reference);CREATE INDEX IF NOT EXISTS idx_verified_refund_order ON commerce_verified_refunds(workspace_id,confirmed_order_id,verified_at,id);
CREATE TRIGGER IF NOT EXISTS trg_return_identity BEFORE UPDATE ON commerce_return_requests WHEN NEW.id<>OLD.id OR NEW.public_reference<>OLD.public_reference OR NEW.workspace_id<>OLD.workspace_id OR NEW.confirmed_order_id<>OLD.confirmed_order_id OR NEW.reason_code<>OLD.reason_code OR NEW.reason_note IS NOT OLD.reason_note OR NEW.customer_scope_hash<>OLD.customer_scope_hash OR NEW.idempotency_key<>OLD.idempotency_key OR NEW.request_hash<>OLD.request_hash BEGIN SELECT RAISE(ABORT,'return identity immutable');END;CREATE TRIGGER IF NOT EXISTS trg_return_delete BEFORE DELETE ON commerce_return_requests BEGIN SELECT RAISE(ABORT,'return history immutable');END;
CREATE TRIGGER IF NOT EXISTS trg_return_guard BEFORE INSERT ON commerce_return_requests WHEN NOT EXISTS(SELECT 1 FROM commerce_orders o WHERE o.id=NEW.confirmed_order_id AND o.workspace_id=NEW.workspace_id) BEGIN SELECT RAISE(ABORT,'return tenant mismatch');END;
CREATE TRIGGER IF NOT EXISTS trg_return_item_identity BEFORE UPDATE ON commerce_return_items WHEN NEW.id<>OLD.id OR NEW.workspace_id<>OLD.workspace_id OR NEW.return_request_id<>OLD.return_request_id OR NEW.confirmed_order_item_id<>OLD.confirmed_order_item_id OR NEW.quantity_requested<>OLD.quantity_requested BEGIN SELECT RAISE(ABORT,'return item identity immutable');END;CREATE TRIGGER IF NOT EXISTS trg_return_item_delete BEFORE DELETE ON commerce_return_items BEGIN SELECT RAISE(ABORT,'return item immutable');END;CREATE TRIGGER IF NOT EXISTS trg_return_item_guard BEFORE INSERT ON commerce_return_items WHEN NOT EXISTS(SELECT 1 FROM commerce_return_requests r JOIN commerce_order_items i ON i.order_id=r.confirmed_order_id WHERE r.id=NEW.return_request_id AND i.id=NEW.confirmed_order_item_id AND r.workspace_id=NEW.workspace_id AND i.workspace_id=NEW.workspace_id AND NEW.quantity_requested<=i.quantity) BEGIN SELECT RAISE(ABORT,'return item mismatch');END;
CREATE TRIGGER IF NOT EXISTS trg_refund_attempt_identity BEFORE UPDATE ON commerce_refund_attempts WHEN NEW.id<>OLD.id OR NEW.workspace_id<>OLD.workspace_id OR NEW.confirmed_order_id<>OLD.confirmed_order_id OR NEW.verified_payment_id<>OLD.verified_payment_id OR NEW.return_request_id IS NOT OLD.return_request_id OR NEW.provider<>OLD.provider OR NEW.requested_amount<>OLD.requested_amount OR NEW.currency<>OLD.currency OR NEW.idempotency_key<>OLD.idempotency_key OR NEW.request_hash<>OLD.request_hash BEGIN SELECT RAISE(ABORT,'refund identity immutable');END;CREATE TRIGGER IF NOT EXISTS trg_refund_attempt_delete BEFORE DELETE ON commerce_refund_attempts BEGIN SELECT RAISE(ABORT,'refund attempt immutable');END;CREATE TRIGGER IF NOT EXISTS trg_refund_attempt_guard BEFORE INSERT ON commerce_refund_attempts WHEN NOT EXISTS(SELECT 1 FROM commerce_orders o JOIN commerce_verified_payments p ON p.id=o.verified_payment_id WHERE o.id=NEW.confirmed_order_id AND o.workspace_id=NEW.workspace_id AND p.id=NEW.verified_payment_id AND p.currency=NEW.currency) OR (NEW.return_request_id IS NOT NULL AND NOT EXISTS(SELECT 1 FROM commerce_return_requests r WHERE r.id=NEW.return_request_id AND r.confirmed_order_id=NEW.confirmed_order_id AND r.workspace_id=NEW.workspace_id)) BEGIN SELECT RAISE(ABORT,'refund lineage mismatch');END;
CREATE TRIGGER IF NOT EXISTS trg_verified_refund_update BEFORE UPDATE ON commerce_verified_refunds BEGIN SELECT RAISE(ABORT,'verified refund immutable');END;CREATE TRIGGER IF NOT EXISTS trg_verified_refund_delete BEFORE DELETE ON commerce_verified_refunds BEGIN SELECT RAISE(ABORT,'verified refund immutable');END;CREATE TRIGGER IF NOT EXISTS trg_verified_refund_guard BEFORE INSERT ON commerce_verified_refunds WHEN NOT EXISTS(SELECT 1 FROM commerce_refund_attempts a WHERE a.id=NEW.refund_attempt_id AND a.workspace_id=NEW.workspace_id AND a.confirmed_order_id=NEW.confirmed_order_id AND a.verified_payment_id=NEW.verified_payment_id AND a.provider=NEW.provider AND a.requested_amount=NEW.verified_amount AND a.currency=NEW.currency AND a.status='VERIFYING') OR NEW.verified_amount>(SELECT p.verified_amount-COALESCE((SELECT SUM(v.verified_amount) FROM commerce_verified_refunds v WHERE v.verified_payment_id=p.id),0) FROM commerce_verified_payments p WHERE p.id=NEW.verified_payment_id) BEGIN SELECT RAISE(ABORT,'verified refund mismatch');END;
`);}};
