export const migration054BusinessBuilderDeploymentHistory={id:54,name:"business_builder_deployment_history",up(db){db.exec(`CREATE TABLE IF NOT EXISTS business_builder_deployments (id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL,project_id TEXT NOT NULL,version_id TEXT,provider TEXT NOT NULL,status TEXT NOT NULL,checksum TEXT,canary_percent INTEGER NOT NULL DEFAULT 0,reason TEXT,metadata_json TEXT NOT NULL DEFAULT '{}',created_at TEXT NOT NULL,updated_at TEXT NOT NULL,FOREIGN KEY(workspace_id) REFERENCES workspaces(id) ON DELETE CASCADE,FOREIGN KEY(project_id) REFERENCES business_builder_projects(id) ON DELETE CASCADE);CREATE INDEX IF NOT EXISTS idx_bb_deployments_workspace_project ON business_builder_deployments(workspace_id,project_id,created_at DESC);`);}};
