export const migration028ImportedFactEventMapping={version:28,name:"imported_fact_event_mapping",up(db){db.exec(`
CREATE TABLE IF NOT EXISTS imported_fact_event_links(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL,imported_fact_id TEXT NOT NULL,business_event_id TEXT NOT NULL,mapping_id TEXT NOT NULL,mapping_version INTEGER NOT NULL,producer_key TEXT NOT NULL,provenance_json TEXT NOT NULL,created_at TEXT NOT NULL,FOREIGN KEY(workspace_id) REFERENCES workspaces(id),FOREIGN KEY(imported_fact_id) REFERENCES canonical_import_records(id),FOREIGN KEY(business_event_id) REFERENCES business_events(id),CHECK(json_valid(provenance_json)),UNIQUE(workspace_id,imported_fact_id,mapping_id,mapping_version),UNIQUE(workspace_id,producer_key));
CREATE INDEX IF NOT EXISTS idx_fact_event_links ON imported_fact_event_links(workspace_id,imported_fact_id);`);}};
