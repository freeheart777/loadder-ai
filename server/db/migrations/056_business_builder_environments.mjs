export const migration056BusinessBuilderEnvironments={version:56,name:"business_builder_environments",up(db){db.exec(`CREATE TABLE IF NOT EXISTS business_builder_environments (workspace_id TEXT NOT NULL,project_id TEXT NOT NULL,environment TEXT NOT NULL,version_id TEXT NOT NULL,commit_ref TEXT,status TEXT NOT NULL,updated_at TEXT NOT NULL,PRIMARY KEY(workspace_id,project_id,environment),FOREIGN KEY(workspace_id) REFERENCES workspaces(id) ON DELETE CASCADE,FOREIGN KEY(project_id) REFERENCES business_builder_projects(id) ON DELETE CASCADE);CREATE INDEX IF NOT EXISTS idx_bb_env_project ON business_builder_environments(workspace_id,project_id);`);}};
