export const migration025KnowledgeCandidates={version:25,name:"knowledge_extraction_candidates",up(db){db.exec(`
CREATE TABLE IF NOT EXISTS knowledge_extraction_candidates(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL,artifact_version_id TEXT NOT NULL,candidate_type TEXT NOT NULL,schema_version TEXT NOT NULL,payload_json TEXT NOT NULL,status TEXT NOT NULL CHECK(status IN('proposed','approved','rejected')),extractor TEXT NOT NULL,extractor_version TEXT NOT NULL,content_hash TEXT NOT NULL,created_at TEXT NOT NULL,FOREIGN KEY(workspace_id) REFERENCES workspaces(id),FOREIGN KEY(artifact_version_id) REFERENCES knowledge_artifact_versions(id),CHECK(json_valid(payload_json)),UNIQUE(workspace_id,artifact_version_id,candidate_type,schema_version,content_hash));
CREATE INDEX IF NOT EXISTS idx_knowledge_candidates ON knowledge_extraction_candidates(workspace_id,artifact_version_id,status);
CREATE TRIGGER IF NOT EXISTS trg_knowledge_candidate_integrity BEFORE INSERT ON knowledge_extraction_candidates WHEN NOT EXISTS(SELECT 1 FROM knowledge_artifact_versions v WHERE v.id=NEW.artifact_version_id AND v.workspace_id=NEW.workspace_id) BEGIN SELECT RAISE(ABORT,'invalid knowledge candidate source');END;
CREATE TRIGGER IF NOT EXISTS trg_knowledge_candidate_update BEFORE UPDATE ON knowledge_extraction_candidates BEGIN SELECT RAISE(ABORT,'Knowledge candidates require reviewed transitions');END;
CREATE TRIGGER IF NOT EXISTS trg_knowledge_candidate_delete BEFORE DELETE ON knowledge_extraction_candidates BEGIN SELECT RAISE(ABORT,'Knowledge candidates cannot be deleted');END;`);}};
