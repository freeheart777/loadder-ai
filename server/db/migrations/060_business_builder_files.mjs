export const migration060BusinessBuilderFiles={id:60,name:"business_builder_files",up(db){db.exec(`CREATE TABLE IF NOT EXISTS business_builder_files(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL,project_id TEXT NOT NULL,app_id TEXT NOT NULL,name TEXT NOT NULL,mime TEXT NOT NULL,size INTEGER NOT NULL,storage_key TEXT NOT NULL,checksum TEXT NOT NULL,visibility TEXT NOT NULL CHECK(visibility IN ('private','workspace','public')),created_at TEXT NOT NULL,deleted_at TEXT,FOREIGN KEY(workspace_id) REFERENCES workspaces(id) ON DELETE CASCADE,FOREIGN KEY(project_id) REFERENCES business_builder_projects(id) ON DELETE CASCADE);CREATE INDEX IF NOT EXISTS idx_bb_files_project ON business_builder_files(workspace_id,project_id,created_at DESC);CREATE UNIQUE INDEX IF NOT EXISTS idx_bb_files_storage_key ON business_builder_files(storage_key);`);}};
