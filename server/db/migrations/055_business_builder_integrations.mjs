export const migration055BusinessBuilderIntegrations={id:55,name:"business_builder_integrations",up(db){db.exec(`CREATE TABLE IF NOT EXISTS business_builder_integrations (id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL,project_id TEXT,kind TEXT NOT NULL,name TEXT NOT NULL,definition_json TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'active',created_at TEXT NOT NULL,updated_at TEXT NOT NULL,FOREIGN KEY(workspace_id) REFERENCES workspaces(id) ON DELETE CASCADE,FOREIGN KEY(project_id) REFERENCES business_builder_projects(id) ON DELETE CASCADE);CREATE INDEX IF NOT EXISTS idx_bb_integrations_workspace ON business_builder_integrations(workspace_id,project_id,updated_at DESC);`);}};
