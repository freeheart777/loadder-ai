export const migration059BusinessBuilderAppInvites={version:59,name:"business_builder_app_invites",up(db){db.exec(`CREATE TABLE IF NOT EXISTS business_builder_app_invites(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL,project_id TEXT NOT NULL,app_user_id TEXT NOT NULL,token_hash TEXT NOT NULL UNIQUE,expires_at TEXT NOT NULL,consumed_at TEXT,created_at TEXT NOT NULL,FOREIGN KEY(workspace_id) REFERENCES workspaces(id) ON DELETE CASCADE,FOREIGN KEY(project_id) REFERENCES business_builder_projects(id) ON DELETE CASCADE,FOREIGN KEY(app_user_id) REFERENCES business_builder_app_users(id) ON DELETE CASCADE);CREATE INDEX IF NOT EXISTS idx_bb_app_invites_lookup ON business_builder_app_invites(workspace_id,project_id,token_hash,expires_at);`);}};