export const migration063BusinessBuilderGitSync={id:63,name:"business_builder_git_sync",up(db){db.exec(`CREATE TABLE IF NOT EXISTS business_builder_git_connections(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL,project_id TEXT NOT NULL,provider TEXT NOT NULL,repository TEXT NOT NULL,branch TEXT NOT NULL,connection_ref TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'active',last_synced_version_id TEXT,last_synced_commit TEXT,created_at TEXT NOT NULL,updated_at TEXT NOT NULL,UNIQUE(workspace_id,project_id,provider,repository,branch),FOREIGN KEY(workspace_id) REFERENCES workspaces(id) ON DELETE CASCADE,FOREIGN KEY(project_id) REFERENCES business_builder_projects(id) ON DELETE CASCADE);CREATE TABLE IF NOT EXISTS business_builder_git_sync_events(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL,project_id TEXT NOT NULL,connection_id TEXT NOT NULL,direction TEXT NOT NULL CHECK(direction IN ('push','pull')),status TEXT NOT NULL,version_id TEXT,commit_ref TEXT,message TEXT,created_at TEXT NOT NULL,FOREIGN KEY(connection_id) REFERENCES business_builder_git_connections(id) ON DELETE CASCADE);CREATE INDEX IF NOT EXISTS idx_bb_git_conn_project ON business_builder_git_connections(workspace_id,project_id);CREATE INDEX IF NOT EXISTS idx_bb_git_events_project ON business_builder_git_sync_events(workspace_id,project_id,created_at DESC);`);}};
