export const migration026ParsingExtraction={version:26,name:"parsed_documents_extraction_runs",up(db){db.exec(`
CREATE TABLE IF NOT EXISTS artifact_inline_payloads(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL,artifact_version_id TEXT NOT NULL,media_type TEXT NOT NULL,content_text TEXT NOT NULL,content_hash TEXT NOT NULL,created_at TEXT NOT NULL,FOREIGN KEY(workspace_id) REFERENCES workspaces(id),FOREIGN KEY(artifact_version_id) REFERENCES knowledge_artifact_versions(id),UNIQUE(workspace_id,artifact_version_id));
CREATE TABLE IF NOT EXISTS parsed_documents(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL,artifact_version_id TEXT NOT NULL,parser_id TEXT NOT NULL,parser_version TEXT NOT NULL,document_type TEXT NOT NULL,language TEXT,locale TEXT,script_direction TEXT CHECK(script_direction IN('ltr','rtl','auto') OR script_direction IS NULL),output_schema_version TEXT NOT NULL,sections_json TEXT NOT NULL,tables_json TEXT NOT NULL,key_values_json TEXT NOT NULL,source_references_json TEXT NOT NULL,warnings_json TEXT NOT NULL,content_hash TEXT NOT NULL,parsed_at TEXT NOT NULL,provenance_json TEXT NOT NULL,FOREIGN KEY(workspace_id) REFERENCES workspaces(id),FOREIGN KEY(artifact_version_id) REFERENCES knowledge_artifact_versions(id),CHECK(json_valid(sections_json)),CHECK(json_valid(tables_json)),CHECK(json_valid(key_values_json)),CHECK(json_valid(source_references_json)),CHECK(json_valid(warnings_json)),CHECK(json_valid(provenance_json)),UNIQUE(workspace_id,artifact_version_id,parser_id,parser_version,content_hash));
CREATE INDEX IF NOT EXISTS idx_parsed_documents_artifact ON parsed_documents(workspace_id,artifact_version_id,parsed_at DESC);
CREATE TABLE IF NOT EXISTS extraction_runs(id TEXT PRIMARY KEY,workspace_id TEXT NOT NULL,artifact_version_id TEXT NOT NULL,parsed_document_id TEXT,parser_id TEXT NOT NULL,parser_version TEXT NOT NULL,extractor_id TEXT NOT NULL,extractor_version TEXT NOT NULL,extraction_schema_version TEXT NOT NULL,status TEXT NOT NULL CHECK(status IN('PENDING','RUNNING','COMPLETED','PARTIAL','FAILED')),input_hash TEXT NOT NULL,warnings_json TEXT NOT NULL,failures_json TEXT NOT NULL,provenance_json TEXT NOT NULL,started_at TEXT NOT NULL,completed_at TEXT,FOREIGN KEY(workspace_id) REFERENCES workspaces(id),FOREIGN KEY(artifact_version_id) REFERENCES knowledge_artifact_versions(id),FOREIGN KEY(parsed_document_id) REFERENCES parsed_documents(id),CHECK(json_valid(warnings_json)),CHECK(json_valid(failures_json)),CHECK(json_valid(provenance_json)),UNIQUE(workspace_id,artifact_version_id,parser_id,parser_version,extractor_id,extractor_version,input_hash));
CREATE INDEX IF NOT EXISTS idx_extraction_runs_artifact ON extraction_runs(workspace_id,artifact_version_id,started_at DESC);`);}};
