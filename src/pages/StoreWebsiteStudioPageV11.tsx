import { useEffect,useMemo,useState } from "react";
import { createPortal } from "react-dom";
import { ArrowsDownUp, Desktop, Eye, EyeSlash, GearSix, MagicWand, Phone, Plus, Trash } from "@phosphor-icons/react";
import { Link } from "react-router-dom";
import StoreWebsiteStudioPageV10 from "./StoreWebsiteStudioPageV10";
import { apiFetch } from "../lib/api";
type Project={id:string;content:Record<string,unknown>};type Product={id:string;name:string;currency:string;basePriceMinor:number;brand?:string|null;category?:string|null;variants?:Array<{inventoryQuantity:number}>};type SectionType="product-slider"|"banner-grid"|"trust"|"text";type Section={id:string;type:SectionType;title:string;enabled:boolean;mode?:"all"|"newest"|"in-stock";subtitle?:string;items?:string[]};type Design={font:string;primary:string;surface:string;text:string;radius:number;productColumns:number};
const seedSections:Section[]=[{id:"s1",type:"product-slider",title:"پیشنهاد ویژه",enabled:true,mode:"in-stock"},{id:"s2",type:"banner-grid",title:"بنرهای کمپین",enabled:true,items:["ارسال رایگان برای خریدهای ویژه","تخفیف محدود آخر هفته"]},{id:"s3",type:"product-slider",title:"جدیدترین محصولات",enabled:true,mode:"newest"},{id:"s4",type:"trust",title:"چرا از ما خرید کنید؟",enabled:true,items:["ارسال سریع","ضمانت بازگشت","پرداخت امن","پشتیبانی"]}];const seedDesign:Design={font:"Vazirmatn",primary:"#6d5dfc",surface:"#ffffff",text:"#111827",radius:18,productColumns:4};const fonts=["Vazirmatn","IRANSansX","Shabnam","Sahel","Dana","Peyda"];async function read(r:Response){const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||"خطا");return d}const money=(n:number,c:string)=>new Intl.NumberFormat("fa-IR").format((n||0)/100)+" "+c;
export default function StoreWebsiteStudioPageV11(){const[project,setProject]=useState<Project|null>(null),[products,setProducts]=useState<Product[]>([]),[sections,setSections]=useState<Section[]>(seedSections),[design,setDesign]=useState<Design>(seedDesign),[panel,setPanel]=useState<"sections"|"appearance"|null>(null),[target,setTarget]=useState<HTMLElement|null>(null),[message,setMessage]=useState(""),[viewport,setViewport]=useState<"desktop"|"mobile">("desktop");useEffect(()=>{void boot()},[]);useEffect(()=>{const locate=()=>{const canvas=[...document.querySelectorAll("section")].find(el=>String(el.className).includes("bg-[#252c38]"));const inner=canvas?.querySelector(":scope > div") as HTMLElement|null;if(inner&&inner!==target)setTarget(inner)};locate();const o=new MutationObserver(locate);o.observe(document.body,{subtree:true,childList:true});return()=>o.disconnect()},[target]);useEffect(()=>{if(!target)return;target.style.transition="max-width .2s ease";target.style.margin="0 auto";target.style.maxWidth=viewport==="mobile"?"390px":"100%"},[target,viewport]);async function boot(){try{const d=await read(await apiFetch("/api/site-projects"));const p=(d.projects||[]).find((x:any)=>String(x.siteType).toUpperCase()==="STORE")||d.projects?.[0];if(!p)return;const detail=await read(await apiFetch(`/api/site-projects/${p.id}`));setProject(detail.project);const saved=detail.project?.content?.storeBuilderV11 as {sections?:Section[];design?:Design}|undefined;if(saved?.sections)setSections(saved.sections);if(saved?.design)setDesign({...seedDesign,...saved.design});const pd=await read(await apiFetch(`/api/stores/${p.id}/products`));setProducts(pd.products||[])}catch(e){setMessage(e instanceof Error?e.message:"خطا")}}async function save(){if(!project)return;try{const r=await read(await apiFetch(`/api/site-projects/${project.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({content:{...project.content,storeBuilderV11:{sections,design}}})}));setProject(r.project);setMessage("ذخیره شد")}catch(e){setMessage(e instanceof Error?e.message:"ذخیره نشد")}}function move(i:number,d:number){const j=i+d;if(j<0||j>=sections.length)return;const n=[...sections];[n[i],n[j]]=[n[j],n[i]];setSections(n)}function add(type:SectionType){setSections(s=>[...s,{id:`sec-${Date.now()}`,type,title:type==="product-slider"?"محصولات منتخب":type==="banner-grid"?"مجموعه بنر":"سکشن جدید",enabled:true,mode:"all",items:type==="trust"?["ارسال سریع","پرداخت امن"]:type==="banner-grid"?["بنر جدید"]:[""]}])}const rendered=useMemo(()=>sections.filter(s=>s.enabled).map((s,i)=><SectionPreview key={s.id} section={s} products={products} design={design} index={i} mobile={viewport==="mobile"}/>),[sections,products,design,viewport]);return <div className="studio-v11" style={{fontFamily:design.font,color:design.text}}><StoreWebsiteStudioPageV10/>{target&&createPortal(<div dir="rtl" style={{background:design.surface}}>{rendered}</div>,target)}<div dir="rtl" className="fixed right-5 top-20 z-[230] flex flex-wrap gap-2"><Link to="/dashboard/websites/quick-start" className="flex items-center gap-2 rounded-xl bg-violet-600 px-4 py-3 text-xs font-black text-white shadow-xl"><MagicWand/> پیش‌نمایش هوشمند</Link><div className="flex rounded-xl bg-[#101722]/95 p-1 text-white shadow-xl"><button onClick={()=>setViewport("desktop")} className={`rounded-lg p-2 ${viewport==="desktop"?"bg-white/15":""}`} title="دسکتاپ"><Desktop/></button><button onClick={()=>setViewport("mobile")} className={`rounded-lg p-2 ${viewport==="mobile"?"bg-white/15":""}`} title="موبایل"><Phone/></button></div><button onClick={()=>setPanel(panel==="sections"?null:"sections")} className="flex items-center gap-2 rounded-xl bg-[#101722]/95 px-4 py-3 text-xs font-black text-white shadow-xl"><ArrowsDownUp/> بخش‌ها</button><button onClick={()=>setPanel(panel==="appearance"?null:"appearance")} className="flex items-center gap-2 rounded-xl bg-[#101722]/95 px-4 py-3 text-xs font-black text-white shadow-xl"><GearSix/> ظاهر</button></div>{panel&&<aside dir="rtl" className="fixed right-5 top-36 z-[240] w-[360px] max-w-[calc(100vw-40px)] max-h-[calc(100vh-165px)] overflow-y-auto rounded-2xl border border-white/10 bg-[#101722] p-4 text-white shadow-2xl">{panel==="sections"?<><b>مدیریت بخش‌های صفحه</b><div className="mt-4 space-y-2">{sections.map((s,i)=><div key={s.id} className="rounded-xl border border-white/10 bg-white/[.03] p-3"><div className="flex items-center gap-2"><button onClick={()=>setSections(x=>x.map(a=>a.id===s.id?{...a,enabled:!a.enabled}:a))}>{s.enabled?<Eye/>:<EyeSlash/>}</button><input value={s.title} onChange={e=>setSections(x=>x.map(a=>a.id===s.id?{...a,title:e.target.value}:a))} className="min-w-0 flex-1 rounded-lg bg-black/20 p-2 text-xs"/><button onClick={()=>move(i,-1)}>↑</button><button onClick={()=>move(i,1)}>↓</button><button onClick={()=>setSections(x=>x.filter(a=>a.id!==s.id))} className="text-red-400"><Trash/></button></div>{s.type==="product-slider"&&<select value={s.mode||"all"} onChange={e=>setSections(x=>x.map(a=>a.id===s.id?{...a,mode:e.target.value as any}:a))} className="mt-2 w-full rounded-lg bg-black/20 p-2 text-xs"><option value="all">همه</option><option value="newest">جدیدترین</option><option value="in-stock">موجود</option></select>}</div>)}</div><div className="mt-4 grid grid-cols-2 gap-2"><Add onClick={()=>add("product-slider")}>اسلایدر محصول</Add><Add onClick={()=>add("banner-grid")}>بنر</Add><Add onClick={()=>add("trust")}>اعتمادسازی</Add><Add onClick={()=>add("text")}>متن آزاد</Add></div></>:<><b>تنظیمات ظاهر</b><div className="mt-4 space-y-4"><label className="block text-xs">فونت<select value={design.font} onChange={e=>setDesign({...design,font:e.target.value})} className="mt-2 w-full rounded-lg bg-black/20 p-3">{fonts.map(f=><option key={f}>{f}</option>)}</select></label><Color label="رنگ اصلی" value={design.primary} onChange={v=>setDesign({...design,primary:v})}/><Color label="پس‌زمینه" value={design.surface} onChange={v=>setDesign({...design,surface:v})}/><Color label="متن" value={design.text} onChange={v=>setDesign({...design,text:v})}/><label className="block text-xs">گردی کارت: {design.radius}px<input type="range" min="0" max="36" value={design.radius} onChange={e=>setDesign({...design,radius:Number(e.target.value)})} className="mt-2 w-full"/></label></div></>}<button onClick={()=>void save()} className="mt-5 w-full rounded-xl bg-violet-600 p-3 text-xs font-black">ذخیره</button>{message&&<div className="mt-3 text-[11px] text-white/55">{message}</div>}</aside>}</div>}
function Add({children,onClick}:{children:React.ReactNode;onClick:()=>void}){return <button onClick={onClick} className="flex items-center justify-center gap-2 rounded-xl border border-dashed border-white/15 p-3 text-xs"><Plus/>{children}</button>}function Color({label,value,onChange}:{label:string;value:string;onChange:(v:string)=>void}){return <label className="flex items-center justify-between text-xs"><span>{label}</span><input type="color" value={value} onChange={e=>onChange(e.target.value)} className="h-9 w-14"/></label>}
function SectionPreview({section,products,design,index,mobile}:{section:Section;products:Product[];design:Design;index:number;mobile:boolean}){if(section.type==="product-slider"){let list=[...products];if(section.mode==="in-stock")list=list.filter(p=>(p.variants||[]).some(v=>v.inventoryQuantity>0));if(section.mode==="newest")list=list.reverse();list=list.slice(0,mobile?4:Math.max(4,design.productColumns));return <section className="px-4 py-8 sm:px-8" style={{background:index%2?"#f8fafc":design.surface}}><h2 className="mb-5 text-xl font-black">{section.title}</h2><div className={`grid gap-3 ${mobile?"grid-cols-2":"grid-cols-4"}`}>{list.map(p=><article key={p.id} className="overflow-hidden border border-slate-200 bg-white p-3" style={{borderRadius:design.radius}}><div className="aspect-square rounded-xl bg-slate-100"/><div className="mt-3 truncate text-sm font-bold">{p.name}</div><div className="mt-2 text-xs font-black" style={{color:design.primary}}>{money(p.basePriceMinor,p.currency)}</div></article>)}</div></section>}if(section.type==="banner-grid")return <section className="px-4 py-8 sm:px-8"><h2 className="mb-4 text-xl font-black">{section.title}</h2><div className={`grid gap-3 ${mobile?"grid-cols-1":"grid-cols-2"}`}>{(section.items||["بنر"]).map((x,i)=><div key={i} className="flex min-h-32 items-end p-5 text-white" style={{borderRadius:design.radius,background:`linear-gradient(135deg,${design.primary},#111827)`}}><b>{x}</b></div>)}</div></section>;if(section.type==="trust")return <section className="px-4 py-8 sm:px-8"><h2 className="mb-5 text-center text-xl font-black">{section.title}</h2><div className={`grid gap-3 ${mobile?"grid-cols-2":"grid-cols-4"}`}>{(section.items||[]).map((x,i)=><div key={i} className="rounded-2xl border bg-white p-4 text-center text-sm font-bold">{x}</div>)}</div></section>;return <section className="px-5 py-9 text-center"><h2 className="text-2xl font-black">{section.title}</h2><p className="mt-3 text-sm text-slate-500">داستان برند، معرفی فروشگاه یا پیام کمپین.</p></section>}