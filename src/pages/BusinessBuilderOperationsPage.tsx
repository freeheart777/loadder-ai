import{useEffect,useState}from"react";import{Link}from"react-router-dom";
type Summary={projects:number;deployments:number;failedDeployments:number;pendingActions:number;runtimeRecords:number;members:number};
export default function BusinessBuilderOperationsPage(){const[s,setS]=useState<Summary|null>(null);useEffect(()=>{fetch('/api/business-builder/operations').then(r=>r.json()).then(d=>setS(d.summary)).catch(()=>setS(null));},[]);const cards=[['اپ‌ها',s?.projects],['استقرارها',s?.deployments],['استقرار ناموفق',s?.failedDeployments],['تأییدهای معلق',s?.pendingActions],['رکوردهای عملیاتی',s?.runtimeRecords],['اعضای فعال',s?.members]];return <main dir="rtl" className="min-h-screen bg-slate-950 text-white p-6"><div className="mx-auto max-w-6xl"><div className="mb-6 flex items-center justify-between"><div><h1 className="text-2xl font-semibold">عملیات اپ‌بیلدر</h1><p className="mt-1 text-sm text-slate-400">وضعیت واقعی ساخت، اجرا و انتشار در یک نگاه</p></div><div className="flex gap-2"><Link to="/dashboard/business-builder" className="rounded-xl bg-white px-4 py-2 text-sm text-slate-950">ساخت اپ</Link><Link to="/dashboard/business-builder/admin" className="rounded-xl border border-slate-700 px-4 py-2 text-sm">ادمین</Link></div></div><section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{cards.map(([label,value])=><div key={String(label)} className="rounded-2xl border border-slate-800 bg-slate-900/60 p-5"><div className="text-sm text-slate-400">{label}</div><div className="mt-2 text-3xl font-semibold">{value??'—'}</div></div>)}</section><section className="mt-6 rounded-2xl border border-slate-800 bg-slate-900/50 p-5"><h2 className="font-medium">اصل لودر</h2><p className="mt-2 text-sm leading-6 text-slate-400">موتور می‌سازد، AI راهنمایی می‌کند. این داشبورد فقط داده‌ای را نشان می‌دهد که از Runtime، Deployment و Governance واقعی آمده باشد.</p></section></div></main>}
