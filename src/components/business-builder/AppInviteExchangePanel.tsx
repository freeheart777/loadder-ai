import{useState}from"react";import{apiFetch}from"../../lib/api";export default function AppInviteExchangePanel({projectId}:{projectId:string}){const[token,setToken]=useState(""),[message,setMessage]=useState("");async function exchange(){const r=await apiFetch(`/api/business-builder/projects/${projectId}/app-invite/exchange`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token})}),b=await r.json();if(!r.ok||!b.success){setMessage(b.message||"دعوت معتبر نیست یا مصرف شده است.");return}localStorage.setItem(`loadder-app-token:${projectId}`,b.session.token);setToken("");setMessage(`دسترسی ${b.session.user?.email||"کاربر"} فعال شد.`);window.dispatchEvent(new Event("loadder-app-session-change"));}return <section className="rounded-2xl border border-white/10 bg-white/[0.025] p-4"><p className="text-xs text-white/35">APP LOGIN / INVITE</p><h3 className="mt-1 font-semibold">ورود با دعوت یک‌بارمصرف</h3><div className="mt-3 flex gap-2"><input dir="ltr" value={token} onChange={e=>setToken(e.target.value)} placeholder="invite token" className="min-w-0 flex-1 rounded-xl border border-white/10 bg-black/25 px-3 py-2 text-xs"/><button disabled={!token.trim()} onClick={exchange} className="rounded-xl bg-white px-4 py-2 text-sm font-semibold text-black disabled:opacity-40">ورود</button></div>{message&&<p className="mt-2 text-xs text-white/50">{message}</p>}</section>}
