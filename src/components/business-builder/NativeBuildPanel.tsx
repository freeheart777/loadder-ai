import{useState}from"react";import{apiFetch}from"../../lib/api";export default function NativeBuildPanel({projectId}:{projectId:string}){const[platform,setPlatform]=useState<'ios'|'android'>('ios'),[manifest,setManifest]=useState<any>(null),[message,setMessage]=useState('');async function build(){const r=await apiFetch(`/api/business-builder/projects/${projectId}/native/${platform}/manifest?capabilities=camera,files,share`),b=await r.json();if(!r.ok||!b.success){setMessage(b.message||'ساخت manifest ناموفق بود');return}setManifest(b.manifest);setMessage(`${platform.toUpperCase()} manifest آماده شد؛ signing/store gate هنوز جداست.`);}function download(){if(!manifest)return;const blob=new Blob([JSON.stringify(manifest,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`loadder-${platform}-manifest.json`;a.click();URL.revokeObjectURL(url);}return <section className="rounded-2xl border border-white/10 bg-white/[0.025] p-4"><p className="text-xs text-white/35">NATIVE TARGET</p><h3 className="mt-1 font-semibold">iOS / Android manifest</h3><div className="mt-3 flex gap-2"><select value={platform} onChange={e=>setPlatform(e.target.value as 'ios'|'android')} className="rounded-xl border border-white/10 bg-[#0b0c0f] px-3 py-2 text-sm"><option value="ios">iOS</option><option value="android">Android</option></select><button onClick={build} className="rounded-xl border border-white/10 px-4 py-2 text-sm">ساخت manifest</button>{manifest&&<button onClick={download} className="rounded-xl bg-white px-4 py-2 text-sm font-semibold text-black">خروجی JSON</button>}</div>{manifest&&<div className="mt-3 grid grid-cols-3 gap-2 text-center text-xs"><div className="rounded-xl border border-white/8 p-2"><b className="block text-lg">{manifest.screens?.length||0}</b><span className="text-white/35">Screen</span></div><div className="rounded-xl border border-white/8 p-2"><b className="block text-lg">{manifest.entities?.length||0}</b><span className="text-white/35">Entity</span></div><div className="rounded-xl border border-white/8 p-2"><b className="block text-lg">{manifest.workflows?.length||0}</b><span className="text-white/35">Workflow</span></div></div>}{message&&<p className="mt-2 text-xs text-white/45">{message}</p>}</section>}
