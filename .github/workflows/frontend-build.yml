name: Frontend Build

on:
  pull_request:
    paths:
      - "src/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig*.json"
      - "vite.config.*"
      - "tailwind.config.*"
      - ".github/workflows/frontend-build.yml"
  push:
    branches: ["feat/**"]
    paths:
      - "src/**"
      - "package.json"
      - "package-lock.json"
      - "tsconfig*.json"
      - "vite.config.*"
      - "tailwind.config.*"
      - ".github/workflows/frontend-build.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Typecheck targeted UI
        run: >-
          npx tsc --ignoreConfig --noEmit --target ES2023 --module ESNext --moduleResolution bundler
          --jsx react-jsx --skipLibCheck --verbatimModuleSyntax true --types vite/client
          src/pages/GoogleAdsSearchWizardPage.tsx
          src/pages/GoogleAdsConnectedWizardPage.tsx
          src/pages/AdsCenterWithGoogleEntryPage.tsx
          src/pages/VisualWebsiteStudioPage.tsx
      - name: Build Vite bundle
        run: npx vite build
