# Master Home prototype — visual review artifacts

Captured from `/prototype/master-home` (dev-only route, fixture data only).
Desktop 1280px and mobile 390px, deviceScaleFactor 1, full page.
Every capture reported zero horizontal overflow.

Reproduce locally:

```
npm run dev:frontend
# then open, per screen:
# http://localhost:5173/prototype/master-home?screen=<id>&chrome=0
```

Screen ids: day1, diagnosis, plan, permission, work, output, replan, result,
learning, mature-full, mature-shortlist.

These images are review artifacts. They are not product assets and nothing here
is production data.
