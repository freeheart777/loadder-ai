# Loadder Brain — visual review captures

From `/prototype/loadder-brain` (dev-only route, fixture data), captured with
`?chrome=0&motion=0` so motion is frozen and images are deterministic.

Desktop 1280px and mobile 390px, three states: new business, growing business,
expert view with a node selected. Zero horizontal overflow in every capture.

Review artifacts, not product assets.
