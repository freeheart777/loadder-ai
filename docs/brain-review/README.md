# Loadder Brain V2 — visual review captures

From `/prototype/loadder-brain` (dev-only route, fixture data), captured with
`?chrome=0&motion=0` so motion is frozen and images are deterministic.

- `desktop-simple` / `mobile-simple` — Simple Brain, ten nodes, everyday language
- `desktop-deep-belief` — Deep Brain with a belief selected: history, gap, links,
  and the link into the existing Decision Room
- `desktop-deep-unknown` — Deep Brain with the empty node selected
- `mobile-deep` — Deep Brain at 390px

Zero horizontal overflow in every capture. Review artifacts, not product assets.
