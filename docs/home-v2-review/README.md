# Home V2 — review captures

`/dashboard` rendered against stubbed canonical reads. Zero horizontal overflow at both widths.

- `after-desktop.png` — four zones at 1280px
- `after-mobile.png` — four zones at 390px, decision above tools
- `before-desktop-legacy.png` — the same build with `?beginner_home_v1=0`

Review artifacts, not product assets.
